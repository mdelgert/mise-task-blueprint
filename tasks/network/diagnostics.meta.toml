# Custom metadata sidecar for diagnostics.toml.
# Excluded from mise task discovery by task_config.excludes in mise.toml.
#
# Uses the same [_.tasks.<name>] namespace mise reserves for arbitrary data in
# normal config files. Fields are validated by tools/validate-blueprint.py --
# see docs/CUSTOM-METADATA.md for the schema.

[_.tasks."network:ping-cloudflare"]
icon = "network"
risk = "low"
enabled = true
tags = ["network", "ping-cloudflare"]
category = "network"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."network:ping-google"]
icon = "network"
risk = "low"
enabled = true
tags = ["network", "ping-google"]
category = "network"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."network:public-ip"]
icon = "network"
risk = "low"
enabled = true
tags = ["network", "public-ip"]
category = "network"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."network:trace"]
icon = "network"
risk = "medium"
enabled = true
tags = ["network", "trace"]
category = "network"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"
