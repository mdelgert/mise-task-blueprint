# Custom metadata sidecar for info.toml.
# Excluded from mise task discovery by task_config.excludes in mise.toml.
#
# Uses the same [_.tasks.<name>] namespace mise reserves for arbitrary data in
# normal config files. Fields are validated by tools/validate-blueprint.py --
# see docs/CUSTOM-METADATA.md for the schema.

[_.tasks."network:ip"]
icon = "network"
risk = "low"
enabled = true
tags = ["network", "ip"]
category = "network"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."network:routes"]
icon = "network"
risk = "low"
enabled = true
tags = ["network", "routes"]
category = "network"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."network:dns"]
icon = "network"
risk = "low"
enabled = true
tags = ["network", "dns"]
category = "network"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."network:ports"]
icon = "network"
risk = "low"
enabled = true
tags = ["network", "ports"]
category = "network"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."network:neighbors"]
icon = "network"
risk = "low"
enabled = true
tags = ["network", "neighbors"]
category = "network"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"
