# Custom metadata sidecar.
# This file is excluded from mise task discovery by task_config.excludes.
# It deliberately uses the same [_.tasks.<name>] namespace supported
# by normal mise config files, while remaining arbitrary TOML for our catalog.

[_.tasks."network:ip"]
icon = "network"
risk = "low"
enabled = true
number = 60
tags = ["network", "ip"]
category = "network"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for network:ip"

[_.tasks."network:routes"]
icon = "network"
risk = "low"
enabled = true
number = 61
tags = ["network", "routes"]
category = "network"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for network:routes"

[_.tasks."network:dns"]
icon = "network"
risk = "low"
enabled = true
number = 62
tags = ["network", "dns"]
category = "network"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for network:dns"

[_.tasks."network:ports"]
icon = "network"
risk = "low"
enabled = true
number = 63
tags = ["network", "ports"]
category = "network"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for network:ports"

[_.tasks."network:neighbors"]
icon = "network"
risk = "low"
enabled = true
number = 64
tags = ["network", "neighbors"]
category = "network"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for network:neighbors"
