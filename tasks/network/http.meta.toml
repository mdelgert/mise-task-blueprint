# Custom metadata sidecar.
# This file is excluded from mise task discovery by task_config.excludes.
# It deliberately uses the same [_.tasks.<name>] namespace supported
# by normal mise config files, while remaining arbitrary TOML for our catalog.

[_.tasks."network:http:headers"]
icon = "network"
risk = "low"
enabled = true
number = 57
tags = ["network", "http", "headers"]
category = "network"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for network:http:headers"

[_.tasks."network:http:json"]
icon = "network"
risk = "low"
enabled = true
number = 58
tags = ["network", "http", "json"]
category = "network"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for network:http:json"

[_.tasks."network:http:weather"]
icon = "network"
risk = "low"
enabled = true
number = 59
tags = ["network", "http", "weather"]
category = "network"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for network:http:weather"
