# Custom metadata sidecar.
# This file is excluded from mise task discovery by task_config.excludes.
# It deliberately uses the same [_.tasks.<name>] namespace supported
# by normal mise config files, while remaining arbitrary TOML for our catalog.

[_.tasks."docker:container:inspect"]
icon = "container"
risk = "low"
enabled = true
number = 34
tags = ["docker", "container", "inspect"]
category = "docker"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for docker:container:inspect"

[_.tasks."docker:container:logs"]
icon = "container"
risk = "low"
enabled = true
number = 35
tags = ["docker", "container", "logs"]
category = "docker"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for docker:container:logs"

[_.tasks."docker:container:shell"]
icon = "container"
risk = "low"
enabled = true
number = 36
tags = ["docker", "container", "shell"]
category = "docker"
destructive = false
requires_sudo = false
interactive = true
anything_you_want = "custom metadata for docker:container:shell"

[_.tasks."docker:ubuntu:run"]
icon = "container"
risk = "medium"
enabled = true
number = 37
tags = ["docker", "ubuntu", "run"]
category = "docker"
destructive = true
requires_sudo = false
interactive = true
anything_you_want = "custom metadata for docker:ubuntu:run"

[_.tasks."docker:ubuntu:create"]
icon = "container"
risk = "low"
enabled = true
number = 38
tags = ["docker", "ubuntu", "create"]
category = "docker"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for docker:ubuntu:create"

[_.tasks."docker:ubuntu:enter"]
icon = "container"
risk = "low"
enabled = true
number = 39
tags = ["docker", "ubuntu", "enter"]
category = "docker"
destructive = false
requires_sudo = false
interactive = true
anything_you_want = "custom metadata for docker:ubuntu:enter"

[_.tasks."docker:ubuntu:stop"]
icon = "container"
risk = "low"
enabled = true
number = 40
tags = ["docker", "ubuntu", "stop"]
category = "docker"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for docker:ubuntu:stop"

[_.tasks."docker:ubuntu:remove"]
icon = "container"
risk = "high"
enabled = true
number = 41
tags = ["docker", "ubuntu", "remove"]
category = "docker"
destructive = true
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for docker:ubuntu:remove"
