# Custom metadata sidecar.
# This file is excluded from mise task discovery by task_config.excludes.
# It deliberately uses the same [_.tasks.<name>] namespace supported
# by normal mise config files, while remaining arbitrary TOML for our catalog.

[_.tasks."system:files:largest-home"]
icon = "computer"
risk = "low"
enabled = true
number = 77
tags = ["system", "files", "largest-home"]
category = "system"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for system:files:largest-home"

[_.tasks."system:files:cwd-size"]
icon = "computer"
risk = "low"
enabled = true
number = 78
tags = ["system", "files", "cwd-size"]
category = "system"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for system:files:cwd-size"

[_.tasks."system:files:find-large"]
icon = "computer"
risk = "low"
enabled = true
number = 79
tags = ["system", "files", "find-large"]
category = "system"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for system:files:find-large"
