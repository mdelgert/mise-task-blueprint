# Custom metadata sidecar.
# This file is excluded from mise task discovery by task_config.excludes.
# It deliberately uses the same [_.tasks.<name>] namespace supported
# by normal mise config files, while remaining arbitrary TOML for our catalog.

[_.tasks."system:pkg:install-core"]
icon = "computer"
risk = "medium"
enabled = true
number = 92
tags = ["system", "pkg", "install-core"]
category = "system"
destructive = false
requires_sudo = true
interactive = false
anything_you_want = "custom metadata for system:pkg:install-core"

[_.tasks."system:pkg:update"]
icon = "computer"
risk = "high"
enabled = true
number = 93
tags = ["system", "pkg", "update"]
category = "system"
destructive = false
requires_sudo = true
interactive = false
anything_you_want = "custom metadata for system:pkg:update"

[_.tasks."system:pkg:search"]
icon = "computer"
risk = "low"
enabled = true
number = 94
tags = ["system", "pkg", "search"]
category = "system"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for system:pkg:search"
