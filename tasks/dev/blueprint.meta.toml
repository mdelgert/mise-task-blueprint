# Custom metadata sidecar for blueprint.toml.
# Excluded from mise task discovery by task_config.excludes in mise.toml.
#
# Uses the same [_.tasks.<name>] namespace mise reserves for arbitrary data in
# normal config files. Fields are validated by tools/validate-blueprint.py --
# see docs/CUSTOM-METADATA.md for the schema.

[_.tasks."dev:blueprint:validate"]
icon = "check"
risk = "low"
enabled = true
tags = ["dev", "blueprint", "validate"]
category = "dev"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."dev:blueprint:manifest"]
icon = "check"
risk = "low"
enabled = true
tags = ["dev", "blueprint", "manifest"]
category = "dev"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."dev:blueprint:ci"]
icon = "check"
risk = "low"
enabled = true
tags = ["dev", "blueprint", "ci"]
category = "dev"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."dev:blueprint:schema"]
icon = "check"
risk = "low"
enabled = true
tags = ["dev", "blueprint", "schema"]
category = "dev"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"

[_.tasks."dev:blueprint:schema:strict"]
icon = "check"
risk = "low"
enabled = true
tags = ["dev", "blueprint", "schema", "strict"]
category = "dev"
destructive = false
requires_confirm = false
requires_sudo = false
interactive = false
cwd_scope = "project"
