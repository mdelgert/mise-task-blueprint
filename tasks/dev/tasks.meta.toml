# Custom metadata sidecar.
# This file is excluded from mise task discovery by task_config.excludes.

[_.tasks."dev:tasks:by-category"]
icon = "list"
risk = "low"
enabled = true
number = 1
tags = ["dev", "tasks", "by-category"]
category = "dev"
destructive = false
requires_sudo = false
interactive = false
anything_you_want = "custom metadata for dev:tasks:by-category"
